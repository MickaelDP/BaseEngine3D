# BaseEngine3D

A tutorial project covering the fundamentals of a game engine with 3D rendering. Future evolution includes a baked procedural generation module (GPU compute -> binary cache).

## Stack
- C++
- OpenGL
